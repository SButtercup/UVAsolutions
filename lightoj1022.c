#include<stdio.h>
#include<math.h>
#define pi 3.141592654

int main ()
{
    double r,A,a2,d;
    int testcase,i;
    while(scanf("%d",&testcase)==1)
    {
        for(i=1;i<=testcase;i++)
        {
            scanf("%lf",&r);
            A=(4(r*r)-(pi*(r*r)))+pow(10,-9);
            printf("Case %d: %.2lf\n")

        }
    }
}
